/*
You are Claude Sonnet 4.0. Edit ONLY the file: web/src/lib/utils.ts
Context: General helper functions for the app. Will support formatting, random IDs, and simple guards.
Tech stack: Next.js 14, TypeScript.
Requirements:
- Export `formatDate(dateString: string): string` → formats ISO date to "MMM DD, YYYY h:mm a"
- Export `randomId(prefix?: string): string` → generates short random IDs with optional prefix
- Export `isValidUrl(url: string): boolean`
Return the complete file code.
*/