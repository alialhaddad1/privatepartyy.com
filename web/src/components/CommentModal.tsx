/*
Task: Generate CommentModal component in React + TypeScript.
File: src/components/CommentModal.tsx

Context:
- Used in Event feed (PostCard.tsx).
- Fetches comments via GET /api/posts/:postId/comments
- Posts new comments via POST /api/posts/:postId/comments
- Props: postId, token (optional)
- Optimistic UI update when adding comment.
- Export default component.
- Return code only.

*/