/*
Task: Generate QRScanner component in React + TypeScript.
File: src/components/QRScanner.tsx

Context:
- Uses camera API to scan QR code.
- On successful scan, navigate to /event/[id]?token=...
- Include fallback manual input.
- Export default.
- Return code only.
*/