/*
Task: Generate Feed component in React + TypeScript.
File: src/components/Feed.tsx

Context:
- Props: eventId, token (optional)
- Fetches posts from /api/events/:id/posts?token=...
- Renders PostCard.tsx for each post.
- Handles likes and refresh logic.
- Return code only. 
*/